{
  "record": {
    "prefix": "record",
    "body": [
      "public record ${TM_FILENAME_BASE}($0) {",
      "}"
    ],
    "description": "record"
  },
  "soutv": {
    "prefix": "soutv",
    "body": [
      "System.out.println(\"${1:var} = \" + $1);$0"
    ],
    "description": "print var to sout"
  }
}
